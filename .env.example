# Cloudflare Tunnel — token do conector (Zero Trust > Networks > Tunnels)
# Copie este arquivo para .env e preencha o token:
#   cp .env.example .env
# No painel do Cloudflare, aponte o public hostname (was.dev.br) para
# o serviço interno do compose: http://web:80
TUNNEL_TOKEN=
